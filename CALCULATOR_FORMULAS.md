# 计算器增益乘区与公式核查表

> 本文对应当前代码实现，主要来源：
> `zzz-hp/src/utils/affixPanelCalc.ts`、`panelBuffCalc.ts`、`damageCalc.ts`。
>
> 约定：所有带 `%` 的输入先除以 100；“百分点加算”表示先把同区百分比相加，再转成乘数。

## 1. 增益字段与乘区归属

### 1.1 面板属性

- `hp`：固定生命；局内生命最终值直接加算。
- `inCombatHpPercent`：局内生命百分比；乘局外生命。
- `externalHpPercent`：局外生命百分比；用于词条模式的局外生命计算，不进入局内 Buff 公式。
- `atk`：固定攻击；局内攻击最终值直接加算。
- `inCombatAtkPercent`：局内攻击百分比；乘局外攻击。
- `externalAtkPercent`：局外攻击百分比；用于词条模式的局外攻击计算，不进入局内 Buff 公式。
- `critRate`：暴击率百分点；进入直伤暴击区。
- `critDmg`：暴击伤害百分点；进入直伤暴击区。
- `penRate`：穿透率百分点；进入防御区。
- `reduceDefense`：无视防御/减防百分点；与 `ignoreDefense` 加算后进入防御区。
- `resPen`：抗性穿透百分点；进入抗性区。
- `mastery`：异常精通固定值；进入异常精通区。
- `anomalyControl`：异常掌控固定值；不进入当前伤害公式。
- `anomalyControlPercent`：异常掌控百分比；乘角色初始异常掌控，不进入当前伤害公式。
- `energyRegen`：能量回复效率百分比；乘角色初始能量回复效率，不进入当前伤害公式。
- `energyRegenFlat`：能量回复效率固定值；直接加算，不进入当前伤害公式。
- `pierce`：固定贯穿力增量；加入贯穿力公式。

### 1.2 通用伤害乘区

- `dmgBonus`：通用增伤百分点；进入增伤区。
- `skillDmgBonus`：特定招式增伤百分点；命中招式条件时与 `dmgBonus` 加算。
- `vulnerable`：易伤百分点；与敌方易伤基础值加算，形成独立易伤区。
- `globalStaggerVulnerable`：全局失衡易伤百分点；失衡期与非失衡期均加到失衡区。
- `staggerVulnerable`：失衡易伤百分点；仅失衡期加到失衡区。
- `staggerVulnerableOnly`：仅失衡期存在的失衡易伤百分点；仅失衡期加到失衡区。
- `special`：特殊补充百分点；与特殊乘区基础值加算，作为独立乘区。
- `pierceDmgBonus`：贯穿增伤百分点；仅基础伤害来源为贯穿力时形成独立乘区。
- `directDmgMult`：直伤倍率百分点；进入直伤倍率区。
- `skillMultiplierBonus`：特定招式倍率百分点；命中招式条件时与 `directDmgMult` 加算。
- `directDmgMultFactor`：直伤倍率修正增量（增益已减 1，默认 0）；与面板倍率修正（默认 1）加算成修正区。
- `anomalyMultFactor`：异常倍率修正增量（已减 1，默认 0）。
- `anomalyReleaseMultFactor`：异放倍率修正增量（已减 1，默认 0）。
- `disorderBaseMultFactor`：紊乱基础倍率修正增量（已减 1，默认 0）。
- `turbulenceBaseMultFactor`：乱流基础倍率修正增量（已减 1，默认 0）。

**综合倍率区**（直伤 / 异放 / 紊乱等）= 加算区 × 倍率修正区。  
加算区来自面板与小类配置的百分点；  
**倍率修正区** = 面板倍率修正（默认 1）+ Σ增益倍率修正增量（已减 1，如 +0.2 填 0.2）。

### 1.3 异常相关乘区

- `anomalyDmgBonus`：异常增伤百分点；进入异常增伤区。
- `anomalyMult`：异常倍率百分点；进入异常倍率区。
- `anomalyCritRate`：异常暴击率百分点；进入异常暴击期望区。
- `anomalyCritDmg`：异常暴击伤害百分点；进入异常暴击期望区。
- `anomalyReleaseDmgBonus`：异放增伤百分点；与普通异常增伤加算进入异放综合增伤区。
- `anomalyReleaseMult`：异放倍率百分点；进入异放倍率区。
- `anomalyReleaseCritRate`：异放暴击率百分点；与普通异常暴击率加算后进入异常综合暴击区。
- `anomalyReleaseCritDmg`：异放暴击伤害百分点；与普通异常爆伤加算后进入异常综合暴击区。
- `disorderBaseMult`：紊乱基础倍率百分点。
- `anomalyDuration`：异常持续时间（秒）；参与紊乱/乱流倍率。
- `disorderCompMult`：紊乱持续时间补偿倍率百分点。
- `disorderDmgBonus`：紊乱增伤百分点；进入紊乱增伤区。
- `turbulenceBaseMult`：乱流基础倍率百分点。
- `turbulenceCompMult`：乱流持续时间补偿倍率百分点。
- `turbulenceDmgBonus`：乱流增伤百分点；与异常增伤百分点加算后进入乱流增伤区。

## 2. 局外面板（词条模式）

### 2.1 生命与攻击

```text
局外生命 =
  角色基础生命
  × (1 + 副词条生命% + 音擎高级生命% + 驱动盘二件套生命% + 主属性生命%)
  + 固定生命副词条
  + 1号位固定生命 2200

局外攻击 =
  (角色基础攻击 + 音擎基础攻击)
  × (1 + 副词条攻击% + 音擎高级攻击% + 驱动盘二件套攻击% + 主属性攻击%)
  + 固定攻击副词条
  + 2号位固定攻击 316
```

### 2.2 异常掌控

```text
局外异常掌控 =
  初始异常掌控
  × (1
     + 音擎高级异常掌控%
     + 驱动盘二件套异常掌控%
     + 6号位异常掌控%)
  + 驱动盘二件套异常掌控（数值）
```

### 2.3 能量回复效率

```text
局外能量回复效率 =
  初始能量回复效率
  × (1
     + 音擎高级能量回复效率%
     + 驱动盘二件套能量回复效率%
     + 6号位能量回复效率%)
  + 驱动盘二件套能量回复效率（数值）
```

### 2.4 其余局外属性

```text
暴击率 = 初始暴击率 + 音擎高级暴击率 + 二件套 + 主属性 + 暴击副词条
暴击伤害 = 初始暴伤 + 音擎高级暴伤 + 二件套 + 主属性 + 暴伤副词条
穿透率 = 初始穿透率 + 音擎高级穿透率 + 二件套 + 主属性
穿透值 = 初始穿透值 + 穿透副词条
异常精通 = 初始精通 + 音擎高级精通 + 二件套 + 主属性 + 精通副词条
增伤 = 初始增伤 + 二件套增伤 + 主属性增伤
```

## 3. 局内最终面板

```text
局内生命 = 局外生命 × (1 + 局内生命%) + 固定生命
局内攻击 = 局外攻击 × (1 + 局内攻击%) + 固定攻击

局内暴击率 = 局外暴击率 + Buff暴击率
局内暴伤 = 局外暴伤 + Buff暴伤
局内增伤 = 局外增伤 + 通用增伤 + 当前招式增伤
局内减防 = 局外减防 + Buff无视防御/减防
局内穿透率 = 局外穿透率 + Buff穿透率
局内抗性穿透 = 局外抗性穿透 + Buff抗性穿透
局内异常精通 = 局外异常精通 + Buff异常精通

局内异常掌控 =
  局外异常掌控
  + 异常掌控（数值）
  + 初始异常掌控 × 异常掌控% / 100

局内能量回复效率 =
  局外能量回复效率
  + 初始能量回复效率 × 能量回复效率% / 100
  + 能量回复效率（数值）

局内直伤倍率 = 局外直伤倍率 + 通用直伤倍率 + 当前招式倍率
```

### 3.1 招式小类倍率

管理端可为招式小类配置独立倍率（未配置异放/紊乱倍率时回落面板）：

```text
直伤倍率区 =
  max(0, 小类直伤倍率% / 100)
  × 小类直伤修正
  × 面板直伤倍率修正（directDmgMultFactor）

异放倍率区 =
  max(0, 小类异放倍率% / 100 或面板异放倍率% / 100)
  × 小类异放修正
  × 面板异放倍率修正

紊乱基础倍率（小类有贡献时）=
  max(0, 小类紊乱倍率% / 100 或面板紊乱基础倍率% / 100)
  × 小类紊乱修正
  × 面板紊乱倍率修正
```

小类配置了非零紊乱倍率或非 1 紊乱修正时，结算展示为「极性紊乱」；否则为「紊乱伤害」。

音擎精炼与其它增益的能量回复统一用百分比字段 `energyRegen`（游戏显示 1.2 时填 120）；旧 `energyRegenFlat` 载入时会迁回百分比。

## 4. 转模结算

转模分两阶段，避免局内面板自引用：

```text
1. 先结算全部非转模 Buff，得到临时局内面板
2. 根据转模来源读取基础值：
   - 局外：读取局外面板
   - 局内：读取临时局内面板
   - 自行：读取用户输入值
3. 转模结果 = 来源值 × 转换比例%
4. 若配置上限：转模结果 = min(转模结果, 上限)
5. 把转模结果加入对应属性后重新生成最终面板
```

## 5. 基础伤害与通用乘区

### 5.1 基础伤害

```text
攻击型基础伤害 = 局内攻击
贯穿型基础伤害 = 0.1 × 局内生命 + 0.3 × 局内攻击 + 固定贯穿力
命破主C固定使用贯穿型基础伤害
```

### 5.2 增伤、暴击、防御、抗性

```text
增伤区 = 1 + clamp(局内增伤% / 100, -0.95, 20)

暴击率 = clamp(局内暴击率% / 100, 0, 1)
暴伤 = clamp(局内暴伤% / 100, 0, 20)
暴击期望区 = 1 + 暴击率 × 暴伤

穿透率 = clamp(局内穿透率% / 100, 0, 0.95)
无视防御率 = clamp(无视防御% / 100, 0, 1)
减防率 = clamp(减防% / 100, 0, 1)
防御保留系数 = max(0, 1 - 无视防御率 - 减防率)

修正后防御 =
  max(0, 敌方防御 × 防御保留系数 × (1 - 穿透率))
  - 穿透值

防御区 = 794 / (794 + 修正后防御)
命破主C防御区固定为 1

抗性区 = 1 - 敌方抗性 + 抗性穿透% / 100
```

敌方抗性预设：

```text
弱点 = -0.2
普通 = 0
抗性20% = 0.2
抗性40% = 0.4
```

### 5.3 易伤、失衡、特殊、贯穿增伤

```text
易伤区 = 敌方易伤基础 + 易伤% / 100

失衡区 =
  敌方失衡基础
  + 全局失衡易伤% / 100
  +（失衡期时）(失衡易伤% + 仅失衡易伤%) / 100

特殊区 = 特殊区基础 + 特殊补充% / 100

贯穿增伤区 =
  基础伤害来源为贯穿力时：1 + 贯穿增伤% / 100
  否则：1
```

### 5.4 通用基础

```text
通用基础 =
  基础伤害
  × 增伤区
  × 防御区
  × 抗性区
  × max(0, 易伤区)
  × max(0, 失衡区)
```

## 6. 直伤公式

```text
直伤倍率区 = max(0, 局内直伤倍率% / 100) × 直伤倍率修正

直伤期望 =
  通用基础
  × 暴击期望区
  × max(0, 特殊区)
  × max(0, 贯穿增伤区)
  × 直伤倍率区
```

有招式小类时，直伤倍率区优先采用小类配置（见 §3.1）。

## 7. 异常公式

### 7.1 异常基础

```text
精通区 = 异常精通 / 100
等级区 = 1 + (代理人等级 - 1) / 59

异常基础 =
  通用基础
  × 精通区
  × 等级区
  × max(0, 特殊区)
```

乱流与异放存在触发角色面板时，异常基础与伤害属性均跟随触发角色；否则使用主C。
异放倍率取自主 C 最终面板：按「当前属性异常的产生角色」属性筛选主 C 上对应属性的异放倍率增益（不再从产生角色面板抄异放倍率）。
可用性限制：乱流伤害仅主C为风属性时计算；异放伤害仅在异放倍率大于 0 时计算。

### 7.2 普通异常

```text
异常增伤区 = 1 + 异常增伤% / 100
异常倍率区 = max(0, 异常倍率% / 100)
异常暴击区（实际期望）=
  1 + (异常暴击率% / 100) × clamp(异常暴伤% / 100, 0, 20)

展示输出两版伤害：
  暴击率=0：异常暴击区 = 1
  暴击率=1：异常暴击区 = 1 + clamp(异常暴伤% / 100, 0, 20)

异常伤害 =
  异常基础
  × 异常增伤区
  × 异常倍率区
  × 异常暴击区（上式两版）
```

### 7.3 异放

```text
异放综合增伤区 = 1 + (异放增伤% + 普通异常增伤%) / 100
异放倍率区 = max(0, 异放倍率% / 100)

综合暴击率 = 异常暴击率% / 100 + 异放暴击率% / 100
综合爆伤 =
  clamp(异常暴伤% / 100, 0, 20)
  + clamp(异放暴伤% / 100, 0, 20)

异常综合暴击区（实际期望）=
  1 + 综合暴击率 × 综合爆伤

展示输出两版伤害：
  暴击率=0：异常综合暴击区 = 1
  暴击率=1：异常综合暴击区 = 1 + 综合爆伤

异放伤害 =
  触发角色异常基础
  × 异放综合增伤区
  × 异放倍率区
  × 异常综合暴击区（上式两版）
```

有触发角色时，伤害属性跟随触发角色（例：爱芮以太打异放、触发简 → 按物理结算并吃物理增益）；异放倍率亦按该属性从主 C 增益中取。

### 7.4 紊乱

有触发角色面板时，异常基础与有效持续时间跟随触发角色（与乱流/异放一致）。

```text
火/以太有效持续时间 = 异常持续时间 / 0.5
其他属性有效持续时间 = 异常持续时间

紊乱倍率区 =
  max(0,
    紊乱基础倍率% / 100
    + 有效持续时间 × 紊乱补偿倍率% / 100)

紊乱增伤区 = 1 + 紊乱增伤% / 100

紊乱期望 =
  触发角色异常基础（有触发时）或主C异常基础
  × 紊乱倍率区
  × 紊乱增伤区
```

展示名称：小类配置了紊乱倍率/修正时为「极性紊乱」，否则为「紊乱伤害」。当前紊乱不额外乘普通异常增伤区、普通异常倍率区或普通异常暴击区。

### 7.5 乱流

```text
乱流倍率区 =
  max(0,
    乱流基础倍率% / 100
    + 有效持续时间 × 乱流补偿倍率% / 100)

乱流综合增伤区 =
  1 + (乱流增伤% + 普通异常增伤%) / 100

异常暴击区 =
  1 + (异常暴击率% / 100) × clamp(异常暴伤% / 100, 0, 20)
  （无普通异常暴击时为 1）

展示输出两版伤害：
  暴击率=0：异常暴击区 = 1
  暴击率=1：异常暴击区 = 1 + clamp(异常暴伤% / 100, 0, 20)

乱流伤害 =
  触发角色异常基础
  × 乱流倍率区
  × 乱流综合增伤区
  × 异常暴击区（上式两版）
```

有触发角色时，伤害属性同样跟随触发角色。

## 8. 当前实现中建议重点核查的事项

1. `externalHpPercent` / `externalAtkPercent` 只在词条模式生成局外面板时使用；面板模式默认认为用户输入的局外面板已经包含它们。
2. 驱动盘二件套局内 Buff 默认不勾选，但词条计算和最优词条分配仍直接纳入二件套局外属性。
3. 6号位异常掌控 30% 与能量回复效率 60% 均按角色初始值乘算。
4. `ignoreDefense` 当前没有对应 Buff 字段；`reduceDefense` 同时承载“无视防御/减防”增益，并与面板 `ignoreDefense` 在防御区加算。
5. 词条模式存在“防御力%”主属性选项，但当前局外面板公式尚未实际把该百分比乘到基础防御力。
6. 紊乱代码当前固定使用主C异常基础；乱流和异放可使用触发角色异常基础，且伤害属性跟随触发角色。
7. `generalMultiplier` 本身不含特殊区、贯穿增伤区、暴击区和直伤倍率区；这些区在对应最终公式中再乘入。
