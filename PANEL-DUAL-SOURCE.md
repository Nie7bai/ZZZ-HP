# 双面板来源改造：面板导入 / 词条导入

> **状态**：方案已定，**未实施**（2026-09-11 与所有者逐条确认）
> **范围**：ZZZ-HP 伤害计算页「角色配置」的局外面板
> **相关**：[计算器增益乘区与公式核查表](./CALCULATOR_FORMULAS.md)、[招式库 / 准备阶段 / 流程改造说明](./skill-flow-redesign.md)

---

## 1. 要解决的问题

同一份局外面板有两个录入入口，产物却塞进**同一个字段**：

| 入口 | 产物 |
|------|------|
| **面板导入** | 用户在游戏里看到 / 手填的数字（截图识别或手打） |
| **词条导入** | 工具按「角色基础 + 音擎 + 驱动盘 + 主属性 + 词条 × 档值」算出的数字 |

两者都写 `anomalySlotPanels[角色]`，只在「确定导入」那一刻写一次。此后：

1. **存的与现推的打岔**：词条页改条数不写回面板字段，于是「面板页看到的」与「词条页看到的」是两个不同的数。
   实测（11维丹派v7）：存的面板 `atk 2915.56 / def 600 / 暴伤 98 / 精通 230` vs 按条数推导 `atk 2401.64 / def 796 / 暴伤 50 / 精通 354`。
2. **下游读面板有三条路**：存的那份 / 按条数现推 / 缺面板时再推导 —— 靠多处 `isAffixMode` 守卫维持，任一处漏判就会静默回落到别的口径。
3. **面板导入还会顺手反推一份词条数**，用于「词条导入」页预填 —— 见 §7，结论是**不靠谱且没必要**，且会静默改写面板。

### 为什么两份本来就该不同

所有者口径（2026-09-11）：

- 游戏面板**隐藏小数**（显示为整数），工具算的是理论精确值；
- 游戏里有些来源工具没有建模，两份数值天然有差；
- 两份可以对应**完全不同的配装**，天差地别是正常的；
- 后续计算产生的小数误差没有办法，接受。

**要求：两份面板同等对待，无视误差，一样处理。伤害计算与词条分配中不需要知道面板是谁给的，只需要知道「我有这个面板」。**

---

## 2. 已确认的决策

| # | 决策 | 说明 |
|---|------|------|
| 1 | 每个角色存**两份面板**，各自带来历 | 面板导入一份、词条导入一份 |
| 2 | **同一时间只有一份激活** | 下游一律读激活那份 |
| 3 | **只有点「确定导入」才写盘并激活** | 编辑中的草稿不落盘、不激活 |
| 4 | 一次导入**只写当前页那一份** | 另一份原样不动，互不干扰 |
| 5 | 激活**按角色独立** | 不是全页面一个；不同角色可用不同来源 |
| 6 | 4/5/6 主属性**跟面板各带一份** | 哪份激活就用它自己的主属性 |
| 7 | 下游只读激活那份，**计算链路不问来历** | 见 §5 |
| 8 | **删除「面板反推词条数」** | 见 §7 |
| 9 | 老数据不标「旧数据」 | 按方案记录的保存模式归位，见 §6 |

### 待确认（本文档实施前请复核）

- **切换入口只列出有数据的那份**：某份为空时不给切过去（避免切到空面板）。若希望允许切到空并回退到「按配装推导」，请指出。

---

## 3. 数据结构

### 3.1 现状

```ts
anomalySlotPanels: Record<agentId, PanelStats>          // 两种来源共用一份
slot.affixCounts: AffixCounts                            // 词条数（可被连续编辑写回）
slot.affixDriveDiscMainStats: AffixDriveDiscMainStats    // 4/5/6 主属性（槽位级一份）
```

### 3.2 目标

```ts
/** 每个角色的面板来源记录 */
type AgentPanelSources = {
  /** 面板导入那份：游戏里看到的数字（截图识别或手打） */
  importedPanel?: PanelStats
  /** 词条导入那份：按条数算出来的数字 */
  affixDerivedPanel?: PanelStats
  /** 生成 affixDerivedPanel 的输入，只服务「词条导入」这一路的再次编辑 */
  affixCounts?: AffixCounts
  affixDriveDiscMainStats?: AffixDriveDiscMainStats
  /** 当前生效的那份 */
  active: 'imported' | 'affixDerived'
  /** 来历与时间，仅展示与排查用；计算链路不得读取 */
  provenance?: { importedAt?: number; source?: 'screenshot' | 'manual' | 'affix' }
}

slotPanels: Record<agentId, AgentPanelSources>
```

要点：

- **两份面板平级**，字段名刻意不叫「主 / 备」，避免再长出主次判断。
- `active` 是每角色一份的显式状态，落进方案库与工作草稿。
- 主属性、词条数挂在各自的来源记录里，不共用。

### 3.3 拆掉旧结构

| 旧 | 新 |
|---|---|
| `anomalySlotPanels[agentId]` | `slotPanels[agentId].importedPanel` / `.affixDerivedPanel` |
| `slot.affixCounts` | `slotPanels[agentId].affixCounts` |
| `slot.affixDriveDiscMainStats` | `slotPanels[agentId].affixDriveDiscMainStats` |

方案快照、工作草稿、导出导入、共享链接等所有持久化路径同步改造，并升 `SCHEME_STORE_VERSION`。

---

## 4. 行为规范

### 4.1 导入（唯一写盘入口）

| 用户在导入弹窗里 | 写入 | 激活 |
|---|---|---|
| 停在「面板导入」页点确定 | `importedPanel` = 手填/识别的面板 | `imported` |
| 停在「词条导入」页点确定 | `affixDerivedPanel` = 按条数算出的面板；同时存 `affixCounts`、`affixDriveDiscMainStats` | `affixDerived` |

- 另一份**原样保留**，不重算、不反推、不清理。
- 导入弹窗内两个子页共用同一次「确定导入」，写哪份由**点确定时所在的子页**决定（现状如此，保留）。

### 4.2 显示

| 位置 | 显示 |
|---|---|
| 导入弹窗 · 编辑区 | **草稿**：随输入实时变；未点确定即未生效 |
| 主页面 · 槽位卡片 | 激活那份（不再随分析结果变化，见已实施部分） |
| 最优词条分配 · 面板口径三行 | 基准 = 激活那份 → +N 条 → 局内 |
| 扫掠柱图 / 伤害计算 | 一律激活那份 |

导入弹窗或槽位卡片上应能看出**当前激活的是哪一份**（两份的来源名、导入时间）。

### 4.3 切换激活

- 允许**手动切换**，无需重新导入。
- 只列出有数据的那份（见 §2 待确认）。
- 切换后全页面立即按新面板重算。

---

## 5. 「一视同仁」的落地

**计算链路零分支**：所有消费面板的函数只接收一份 `PanelStats`，不接收、不查询它的来源。

- 删掉 `PanelCalcSection` 里按 `isAffixMode` 分流读面板的逻辑（现 9 处使用 + `calcMode === 'affix'` 分支）；
- `optimalAffixAlloc.ts` 的基准面板、`panelBuffCalc.ts` 的槽位局外，都改为读激活那份；
- 来源信息（`provenance`）**只是元数据**：用于界面显示和排查，任何计算函数不得读取。

> 这条是防止将来又长出分支的保险：**要区分来源，就说明结构没设计对。**

### 侧面约束的地位（不进伤害公式）

| 数据 | 作用 | 是否进公式 |
|---|---|---|
| 4/5/6 主属性 | 扫掠柱图的副词条上限（`36 − 6 × 同类主属性数`） | 否 |
| 词条数 | 生成「词条导入」那份面板 | 否 |

两者都跟各自那份面板一起存，只服务各自那一路。
（词条最优分配不复用柱图上限规则，此为 2026-09-10 既定决策，本次不变。）

---

## 6. 迁移

现有方案只有一份面板，来历信息当年没有存下来，**只能推断**：

| 方案记录的 `panelCalcMode` | 归位 |
|---|---|
| `affix` | `affixDerivedPanel`，`active = 'affixDerived'` |
| 其他（`panel`） | `importedPanel`，`active = 'imported'` |

`affixCounts` / `affixDriveDiscMainStats` 一并迁入该来源记录。

### 为什么只能推断（实测证据）

| 方案 | 记录的 `panelCalcMode` | 存的面板 vs 按条数推导 |
|---|---|---|
| 克珂丽 | `affix` | 完全一致 |
| 11维丹派v7 | `affix` | 不一致（攻击/防御/爆伤/精通/穿透 全不同） |

两者都记为 `affix`，但派派那份面板并非由其条数算出 —— 该字段记录的是**保存方案时页面停在哪个标签**，不等于面板的写入路由（导入后切标签、或改条数未重新导入，都会让它与实际来历不符）。

**归错的后果**：只影响「下次从哪个入口导入会覆盖它」；**数值与计算不受影响**（§5 一视同仁）。因此不额外标注「旧数据」，按上表归位即可。

---

## 7. 删除「面板反推词条数」

### 7.1 实验证据（`scripts/probe-affix-inference.mjs`）

| 实验 | 结果 |
|---|---|
| **[A] 往返保真** | 条数 → 面板 → 反推条数，**6/6 完全还原**（模型自洽时算法是精确的） |
| **[B] 扰动敏感** | 面板混入模型外的量（+2.4 暴击 / +4.8 爆伤 / +18 精通 / +19 攻击）→ 反推分别错成 11 / 9 / 8 / 1 条（真值 10 / 8 / 6 / 0），**全部无警告** |
| **[C] 多解性** | 穷举「局外大攻击 × 攻击力」只有 1 组能复现目标面板 —— **不是解不唯一，是模型对不上现实** |
| **[D] 关键后果** | 面板导入后重开弹窗、切到「词条导入」页点确定 → 面板按反推条数重算，实测暴击率 **30.0% → 29.0%**，静默改写、无提示；在「面板页」点确定则不会改写 |

[B] 的原因：暴击 / 爆伤 / 穿透 / 精通四项只做一次除法，**没有残差校验**；生命 / 攻击虽算了残差警告，但两个调用点都丢弃了返回值里的 `warnings`。

### 7.2 结论与动作

- **不靠谱**：会静默出错，且无法察觉；
- **没必要**：反推出的条数在面板这一路上没有任何下游消费者（伤害计算直接用面板、词条被跳过），唯一用途是「词条导入」页的预填；
- **有害**：[D] 的静默改写。

动作：

1. 删除 `inferAffixCountsFromExternalPanel` 及其两个调用点（`UnifiedPresetPicker`、`PanelCalcSection`）；
2. 「词条导入」页不再预填反推值 —— 条数只来自该角色 `affixCounts`（没有则从空开始）；
3. 将来若要做「从面板帮你估个起点」，重写并满足：带残差警告、只作参考、**绝不回写面板**。

---

## 8. 非目标

- **转模面板**（`convertSlotPanels`）不在本次范围，维持现状；
- 词条最优分配的求解器约束（不限上限）不变；
- 招式 / 流程 / 增益相关逻辑不动。

---

## 9. 验收标准

1. 面板导入后，界面任何位置显示的面板都是**填进去的那份**（含小数），不出现"按条数现推"的另一个数；
2. 词条导入后，手动切到另一份再切回，**数值不被改写**；
3. 在「词条导入」页改条数**不点确定**：主页面与一切计算均不变；
4. 切换激活后面板显示、扫掠柱图、最优分配、伤害计算**同一次重算内一致**（不得出现两条链路各算各的）；
5. 计算链路中不存在读取来源信息的分支（代码审查）；
6. 老方案导入后数值与迁移前**逐位一致**；
7. 现有 20 个测试脚本全过；`type-check`、`eslint` 干净。

---

## 10. 实施清单（待排期）

| 区域 | 文件 | 事项 |
|---|---|---|
| 状态与持久化 | `types/damageCalcHistory.ts`、`utils/damageCalcHistory.ts` | 新结构 + 迁移 + 升版本 |
| 页面状态 | `components/calculator/DamageCalcPage.vue` | `slotPanels` 替代 `anomalySlotPanels`；激活状态随方案/草稿 |
| 导入写入 | `components/calculator/UnifiedPresetPicker.vue` | 按所在子页写对应那份；删除反推调用 |
| 面板消费 | `components/calculator/PanelCalcSection.vue` | 删除 `isAffixMode` 分流（9 处 + 1 分支）；删除反推调用 |
| 最优分配 | `utils/optimalAffixAlloc.ts` | 基准面板读激活那份 |
| 增益计算 | `utils/panelBuffCalc.ts` | 槽位局外读激活那份 |
| 界面 | 槽位卡片 / 导入弹窗 / 最优模块 | 显示当前激活来源；切换入口 |
| 删除 | `utils/affixPanelCalc.ts` | 删除 `inferAffixCountsFromExternalPanel` |

---

## 附：已实施的前置改动（本方案之外，已完成）

| 提交 | 内容 |
|---|---|
| `408167d` | 不再剔除主 C 面板，让「角色配置」基准真正生效 |
| `46799f4` | 柱图与详情的总伤不再因缓存串值而分叉；扫掠复用分支补主词条上限 |
| `701906e` | 删掉槽位上只写不读的 `isMainC` 标记 |
| `fbe6bea` | 环境 Buff 内容变化后不再沿用旧缓存；文档同步异常类锚点规则 |
| `62e83d8` | 分析结果不再回显到槽位卡片，改为最优模块内「面板口径」三行 |
| `40544b4` | 反推探针 `probe-affix-inference.mjs`；修正 OCR 失败提示的陈旧说法 |
