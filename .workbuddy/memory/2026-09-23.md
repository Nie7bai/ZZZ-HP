# 2026-09-23

## 拉取远程更新（feature/affix-alloc-rework）

- 从 origin(GitHub) fetch 发现本地落后 12 个提交，用 `git pull --ff-only` 快进到 d6a49cd，无冲突。
- 变更集中在词条最优分配：组管理「不占词条数」开关（前后端）、游戏专用规则额度口径、相关测试脚本。未涉及 package.json，无需重装依赖。
- 关键环境坑：git 默认走了失效的系统代理 127.0.0.1:7899，导致 `SSL_ERROR_SYSCALL` / `HTTP2 framing` / `CONNECT tunnel failed 502`。直连 GitHub 其实是通的。解决办法：执行 git 网络操作前清空 `http_proxy/https_proxy/ALL_PROXY` 并加 `-c http.proxy= -c https.proxy= -c http.version=HTTP/1.1`。
