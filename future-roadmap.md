# 招式库 / 流程 · 未来改造

> **现在不做，不是否决。** 不要在日常施工里主动开这里的条目，除非用户点名。  
> 方案正文：[`skill-flow-redesign.md`](./skill-flow-redesign.md)。本阶段待办：[`skill-flow-todo.md`](./skill-flow-todo.md)。  
> 记下：2026-08-14。顺序 = 以后动手的先后，**第 1 条最先**。

结算骨架已经能跑。下面这些是产品/录入模型还没跟上，或要动别的模块才动得了。

---

## 1. 管理端增益：先有「招式名」，招式库再挂对应锚点

**现状不影响结算**，先不管。但这是管理端录入和方案口径差得最大的一块，以后改限定项 UI 时按这条做。

### 该是什么样

用户口径（2026-08-14 核对过，和方案 §4.2 一致）：

1. **技能（增益）里先有「招式名」。**  
   管理员配一条要打到「某一招」的 Buff 时，当场给这一招起名并存下来。工具里这个名字叫**增益锚点**（旧表 `SkillSubcategory`，id 沿用 `subcategoryId`）。它长得像游戏里的真实招名，但**不是**招式库那条招式的显示名。
2. **招式库里后挂同一个锚点。**  
   用户（或以后的预设招式）建直伤招式时，从「该角色已有的锚点」里选中这一条。Buff 靠锚点 id 认出这条招式，不靠招式显示名字符串对上。
3. **顺序是因 Buff 而生，不是因招式清单而生。**  
   没有「先扫一遍招式库再给 Buff 下拉」这条路。86 条角色锚点**不是**招式清单，也**不要**用它们起草预设直伤（已否决）。
4. **招式类型和锚点互不干涉。**  
   限定某一招 = 限定这个锚点；限定某一类招 = 限定招式类型（可多选，如普攻、追加攻击）。不是「先选大类再选小类」的父子关系。
5. **招式可以不挂锚点。** 自建招式叫什么都行，显示名和锚点无关。异常类招式类型、锚点都空，招式限定 Buff 天然打不中。

管理端以后应改成：**限定招式类型，或限定锚点，二选一（可多条）**。在 Buff 编辑器里新建锚点：填角色 + 名字即可，**不要再强制挂一个招式大类**。

### 现在管理端为什么不符合

计算页招式库已经按「类型 × 锚点」在用。管理端 Buff 效果编辑器还是旧模型：

| | 方案 / 计算页 | 管理端现在（`AdminBuffEffectEditor`） |
|--|----------------|--------------------------------------|
| 点名某一招 | 锚点（独立，不隶属类型） | 「招式小类」，必须先选「招式大类」 |
| 点名某一类 | 招式类型（多选） | 「整大类」= 小类留空 |
| 追加攻击 | 招式类型之一，和普攻同级 | 塞进「招式大类」下拉里的**伪大类** `follow_up`，小类显示「全部追加」 |
| 新建锚点 | 角色 + 名字 | 仍要选「招式大类」，还可勾「视为追加攻击」 |

所以管理员会看到：大类列表末尾有「追加攻击」，小类是「全部追加」。这**不是**这次招式库改造加进去的，是旧 Buff 限定项就有的伪大类。结算已经改成只认招式是否勾了类型「追加攻击」；管理端这套下拉暂时还能靠适配层对上旧数据，**先不动**。

相关文件（以后改录入 UI，不是先改库数据）：

- `ZZZ-HP/zzz-hp/src/components/admin/calculator/AdminBuffEffectEditor.vue`（限定项仍 `{ category, subcategoryId }`）
- `ZZZ-HP/zzz-hp/src/components/admin/calculator/AdminSkillSubcategoryPanel.vue`（侧栏已写「招式小类 / 增益锚点」，编辑器内部仍是大类+小类）
- `ZZZ-HP/zzz-hp/src/types/calculator.ts`：`BUFF_SKILL_TARGET_OPTIONS` 把 `follow_up` 附在真实大类后面

### 以后动手时注意

- 现有 Buff 限定、小类、追加规则库**可以继续零改动**，靠现在的「类型 → 旧坐标 / `isFollowUp`」适配层。改的是录入 UI 和术语。若要把 3 条公共小类真正提升成类型，那是下面第 3 条，会动库，不要和本条绑死一次做完。
- `countsAsFollowUp` / `FollowUpSkillRule` 已不参与结算；管理端「视为追加攻击」「整大类算追加」是旧录入残留，改限定项时一起拿掉或改成勾招式类型。
- 计算页招式的显示名不要自动改成锚点名。

---

## 2. 三条流程合成一条时间轴

三人各有一块同构的准备 / 流程，条目带归属，是为以后合并显示预留的。

现在按人分开排。以后若做成一条时间轴，应是三个 `flow` 数组合并，不要把三人做成三种数据结构。

目前没有产品级实现条件。

---

## 3. 三条公共小类真正提升为招式类型

冲刺攻击 / 闪避反击 / 强化特殊技：类型清单里已经能勾，结算把它们翻译回旧公共小类 id。库里那 3 条记录还在。

以后若彻底清理：把这 3 条从「假小类」提升成真正的招式类型，并重映射约 112 条 Buff 限定。**会改库。** 现在靠适配层，库一行不动。不要和本文件第 1 条（只改管理端录入 UI）混成一次提交。

---

## 4. 页级「默认失衡」和流程行失衡对齐 (样式问题)

流程每一行可以勾是否失衡。局内**通用** Buff 仍看页级「默认失衡」下拉，没有跟行走。

拆开要对齐其它模块（通用面板、异常、最优词条等），2026-08-14 明确先不动。

---

## 5. 空列表引导加强、准备招式拖进流程(注释说明问题)

列表空时已有短提示。加强空态引导、以及从准备阶段把招式拖进流程，2026-08-14 明确先不做。

---

## 6. 流程伤害统计：美化与补全

2026-08-18 已在招式流程模块两列**下面**：全队总伤（左）+ 当前角色总伤、三人分伤、按类型/失衡两张饼图。伤害记录全局一份，手点才记，切方案不换；带方案名/角色名，长名省略悬停看全称；最多 10 条。

还没做、以后补：

- UI 按 zzz-dev 再排一版（字号、间距、占比分析弹窗）
- 更多饼图维度（招式名、招式类型、异常小类等）
- 完整「伤害计算和提升」（词条/乘区扰动对比，不是现在这条记录表）

---

## 笼统待办

> 和其他条目分开。下面是用户原文分类，还没有拆成可施工的规格。

1. 优化排版界面和布置顺序，具体是编辑装备行为和流程行为
2. 优化词条计算，独立出去，增加功能和自由度
3. 增加流程排版功能，组合便于排版
4. 优化 Buff 选择，区分失衡期 ，进阶：结合流程改变buff生效时机

