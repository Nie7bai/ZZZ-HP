# 伤害事件「完全跟随方案」— 实现方案

> 历史目标分支：`3.1.6.3`（当时基于已并入的 `3.1.6.4` 半成品）
> 当时约束：`3.1.6.4` 保持不动
> 日期：2026-08-12（落地更新：2026-08-13）  
> 对照说明：当时 `78291f2` 写的「由全局存储改为跟随方案」只实现了一半
> 文档状态：历史实现记录，不定义当前分支或提交方向；现行规则见 [`docs/policies/git-workflow.md`](./docs/policies/git-workflow.md)。

---

## 0. 本分支已落地（`3.1.6.3`）

- 关自动写全局；迁移不再合并灌空方案；只打 `customEventsMigrated`
- 保存：「存为全局自定义」/「更新到全局」；「存为方案事件模式」（无方案则「新建或加载方案」或改存全局）
- 侧栏「+ 编辑事件」；未落盘不标方案/全局；已绑定方案才标「· 方案」
- 方案字段 `directEventModeName` / `anomalyEventModeName`；进入页不默认加载方案
- 「取消当前方案」解除绑定；确认框独立 Teleport；方案库 z-index 高于事件窗
- 摘要条 / 去向弹窗 / 取消钮白天主题对齐

历史合入说明见 [`docs/branch-history.md`](./docs/branch-history.md) 中的 `3.1.6.3` 记录。

---

## 1. 目标口径（完成定义）

### 做完后应满足

1. **真相来源 = 方案**  
   每个方案自己的 `directEvents` / `anomalyEvents`（及可选的 `damageKind` / `staggerPhase`）是唯一持久化真相。
2. **切方案只恢复该方案事件**，不改写其它方案，不污染全局模式库。
3. **全局「自定义模式 / 管理员预设」= 模板库**  
   仅用于「载入到当前页」或「从当前页另存为模板」；编辑方案事件时**默认不**自动写回全局库。
4. **新建事件不必先建全局模式**  
   可在当前方案内直接增删改事件，保存方案即落盘。
5. **不再出现**：空方案被灌入「所有模式事件总和」；切方案把同名全局模式覆盖成另一套。

### 非目标（本阶段可不做）

- 删除用户浏览器里已有的全局模式数据（可保留只读兼容，后续版本再清）
- 云端同步方案库
- 改伤害公式本身

---

## 2. 现状（半成品）

| 能力 | 状态 |
|---|---|
| 方案字段存 `directEvents` / `anomalyEvents` | 已有 |
| 加载 / 保存方案读写上述字段 | 已有 |
| 切方案清 `modeId` / `modeName` | 已有（`42d8803` / `3f69c52`） |
| `customEventsMigrated` 持久化防重复迁移 | 已有（`549167b`） |
| 迁移仍合并「全部全局模式事件」灌空方案 | **仍在，需改** |
| 选中自定义模式时 `watch(events)` 自动 `upsertCustomMode` | **仍在，需改** |
| 方案不存 `modeId`；新建入口仍偏「建全局模式」 | **未对齐口径** |

---

## 3. 目标架构

```
┌─────────────────────────────────────────┐
│ 方案 Scheme（localStorage 方案库）        │
│  directEvents / anomalyEvents           │  ← 唯一真相
│  damageKind / staggerPhase              │
│  （可选）directModeRef / anomalyModeRef │  ← 仅记录「从哪个模板载入过」，不反向覆盖
└─────────────────────────────────────────┘
                    ▲ 保存/加载
                    │
┌─────────────────────────────────────────┐
│ 计算页当前事件（内存）                    │
│  directEvents / anomalyEvents           │
│  modeId 可为空 = 方案内编辑，非模板绑定   │
└─────────────────────────────────────────┘
         │ 载入模板（可选）    │ 另存为模板（显式）
         ▼                    ▼
┌─────────────────────────────────────────┐
│ 模板库（全局，只读为主）                  │
│  管理员预设 + 自定义模式                  │
│  禁止：编辑方案事件时自动 upsert          │
└─────────────────────────────────────────┘
```

---

## 4. 实现任务清单

### P0 — 行为纠偏（必须）

#### P0-1. 关闭「编辑即写全局」
**文件：** `DamageEventModeModal.vue`

- 删除或默认关闭 `watch(events) → persistCurrentCustom()` 自动持久化。
- 改为显式按钮：
  - **另存为自定义模式**
  - **更新当前自定义模式**（仅当用户明确点「同步到模板」时）
- 管理员预设：保持「会话可改、不写库；另存才进自定义」。

#### P0-2. 方案内直接维护事件
**文件：** `DamageEventModeModal.vue` / `DamageCalcPage.vue`

- 增加「在本方案编辑」路径：无 `modeId` 也可增删改事件。
- 摘要条：
  - 有事件且无 modeId →「方案事件 · N 条」（不要显示成误导性的「自定义模式」）
  - 从模板载入且仍绑定 →「模板名 · N 条」
- 「+ 新建」优先含义改为：**为本方案新建空事件列表**（可不创建全局模式）；若需模板再「另存为自定义」。

#### P0-3. 重写一次性迁移
**文件：** `damageCalcHistory.ts` → `migrateLegacyGlobalEvents`

**禁止：** 多模式事件 `push` 合并后灌进每个空方案。

**改为（推荐）：**

1. 若 `customEventsMigrated` 已 true → return（保持）。
2. 标记 `customEventsMigrated = true`。
3. **默认不自动灌事件**；空方案保持 `[]` / 字段缺失。
4. （可选增强）若产品坚持「给旧用户带一份」：每个空方案只拷贝**一个**默认模式（例如最近修改、或同主 C 的第一个），并写入可选的 `*ModeRef`；绝不合并。

区分字段语义：

- `undefined`：从未有过方案事件 → 才允许可选默认填充  
- `[]`：用户已清空 → **永不**再灌

#### P0-4. 保存 / 加载契约写死
**文件：** `DamageCalcPage.vue`

- `saveHistoryEntry` / `overwriteHistoryEntry`：始终写入当前页事件数组，并写入 `directEventModeName` / `anomalyEventModeName`（展示用）。
- `loadHistoryEntry`：先清 `modeId`（不断开到全局自动写回），再恢复方案事件与模式展示名；加载后**不要**根据全局库再改写事件。
- 进入计算器**不**默认加载 / 高亮上次或首位方案；仅用户点「加载」后才套用。
- 切方案**不要**自动覆盖保存上一方案（除非产品另加「离开前提示保存」——本阶段可不做自动写）。

### P1 — 产品对齐（建议同版本做完）

#### P1-1. 方案可选记录模板引用
**类型：** `DamageCalcHistoryEntry`

```ts
directEventModeId?: string | null
directEventModeName?: string | null
anomalyEventModeId?: string | null
anomalyEventModeName?: string | null
```

- 从模板「载入」时写入这些字段 + 拷贝 events 到方案字段。
- 加载方案时恢复 mode 显示名（仅展示）；**即使全局模板已变，仍以方案内 events 为准**。
- 「更新到模板」才把方案 events 写回对应 `modeId`。

#### P1-2. 文案与引导
- 方案库 / 事件弹窗说明改为：「事件跟方案保存；自定义模式是可选模板。」
- 去掉或改写 `78291f2` 类说明中「后续请按方案维护」却仍引导全局模式为主的矛盾文案。

#### P1-3. 脏数据清理（工具，一次性）
可选脚本或设置页按钮：

- 扫描方案：若 `anomalyEvents`/`directEvents` 明显等于「全局库全量拼接」→ 提示用户清空或重置该方案事件。
- **不要**静默改用户数据；需确认。

### P2 — 后续版本

- 大版本 4：移除 `migrateLegacyGlobalEvents` 与 `customEventsMigrated`（已有 `@temporary` 注释）。
- 评估是否停止把全局模式当默认入口，仅保留「从模板载入 / 另存模板」。

---

## 5. 关键文件

| 文件 | 改动 |
|---|---|
| `zzz-hp/src/components/calculator/DamageEventModeModal.vue` | 关自动 persist；摘要文案；新建=方案内编辑 |
| `zzz-hp/src/components/calculator/DamageCalcPage.vue` | 保存/加载契约；摘要联动；可选 modeRef |
| `zzz-hp/src/utils/damageCalcHistory.ts` | 重写迁移；禁止合并灌入 |
| `zzz-hp/src/types/damageCalcHistory.ts` | 可选 modeRef 字段 |
| `zzz-hp/src/utils/customDamageEventModes.ts` | 保持；仅被显式另存/更新调用 |
| `docs/branch-history.md` | 保留原 `3.1.6.3` 分支的历史合入说明 |

---

## 6. 验收用例

1. **方案隔离**  
   方案 A 配 3 条事件并保存 → 加载空方案 B → B 为 0 条（或 B 自己旧数据），侧栏可见模板但不等于 B 已保存。
2. **不自动写全局**  
   加载 A，无 modeId，改事件 → 全局自定义库条数/内容不变 → 覆盖保存 A → 再加载 A 为改后内容。
3. **显式另存模板**  
   在 A 上「另存为自定义」→ 全局多一个模板；加载 B 再载入该模板 → 只有当前页变；保存 B 后 B 才有这份。
4. **更新模板需确认**  
   绑定某自定义模式时改事件 → 不自动改全局；点「更新到模板」后全局才变；其它已保存方案的 events 不变。
5. **切方案不污染**  
   A 选中自定义「甲」→ 切 B → 「甲」全局内容不变；B 显示未绑定或 B 自己的引用。
6. **迁移**  
   新档案 / 新空方案不会再出现「固定 15 条合并事件」。
7. **回归**  
   管理员预设仍可载入；导出/导入方案包仍带 events。

---

## 7. 历史提交拆分

以下是 2026-08-12 原计划在 `3.1.6.3` 上采用的提交拆分：

1. `fix(calculator): 事件编辑默认不自动写回全局模式库`  
2. `fix(calculator): 迁移不再合并灌入空方案`  
3. `feat(calculator): 方案内直接维护事件与摘要文案`  
4. `feat(calculator): 方案可选记录事件模板引用`（若做 P1-1）  
5. `docs: 更新方案事件实现说明`

当时的合入方向是保留 `3.1.6.4`，并将后续完整实现提交到 `3.1.6.3`。该安排只用于理解历史，不再约束当前开发。

---

## 实现备注（2026-08-12 落地口径）

用户补充：
- **原有**全局自定义模式保留在全局库。
- **新建**事件提供两种显式保存：
  1. 存为全局自定义模式
  2. 跟随当前方案：已有当前方案则覆盖写入；无则提示先新建方案；否则只能存全局。
- 编辑过程**不再**自动 `upsert` 全局库。
- 迁移**不再**把多模式事件合并灌进空方案。
